apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: odin-api-tls
spec:
  secretName: odin-api-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: odin-api.dev.gothor.com
  dnsNames:
  - odin-api.dev.gothor.com
  acme:
    config:
    - http01:
        ingress: thor-api
      domains:
      - odin-api.dev.gothor.com
